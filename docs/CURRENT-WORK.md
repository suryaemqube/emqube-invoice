# Current Work

Keep this short and current — it's read every session. Update it as work actually progresses; a stale entry here is worse than no empty one, since it actively misleads the next session.

## Status as of this writing

**Phase 3 (Shared Components) + Phase 4 (Invoice List) + Option A polish + Create/Edit Invoice + User area (login, role-driven menu, role gating, change password) + Quote List + Create/Edit Quote + Customer List (list + add/edit modal) are complete.** The app builds, type-checks clean, and its 6 existing tests pass.

> **Improvements log (2026-09-01):** every improvement / bug fix / cleanup made so far is documented in `docs/IMPROVEMENT-LOG.html`, organised by functional area (login → change password → sidebar → profile → UAT/LIVE config → invoice list → create/edit invoice → copy → quote list → create/edit quote) — each entry records **what** was done and **why** (see **Deliberate deviations** in `DECISIONS.md` for the quote-revision / VAT / discount behaviour). **Customer List (2026-09-02)** section added below.

### Customer List built (`src/app/features/customers/pages/customer-list/`)

- **Route:** `app.routes.ts` — `/customerlist` (the menu's `PageUrl`) and the leftover `/customer` both load the real `CustomerList` component (both were placeholders). New `features/customers/` folder with `services/customer.service.ts` + `pages/customer-list/`.
- **Models** (`models/invoice.model.ts`): added the full backend shapes `CustomerListModel`, `CustomerModel` (extends the list fields + `SiteUserId`, `CustomerTypeValue`, `BillingAddress`, `ShippingAddress`), `AddressModel`, `CountryModel`, `RCountryList`, and `emptyCustomerModel()` / `emptyAddress()` factories. (The pre-existing `CustomerDetail`/`CustomerOption` used for invoice/quote form auto-fill are untouched.)
- **Service:** `customer.service.ts` — `getCustomerList()` (`Invoice/GetCustomerList` → `CustomerListModel[]`), `getCustomer()` (`Invoice/GetCustomer`), `addUpdateCustomer()` (`Invoice/AddCustomer`, posts `CustomerModel`), `getCountries()` (`Common/GetCountry` → `RCountryList`), `getStates(countryId)` (`Common/GetState`), `getCurrencies()` (`Invoice/GetCurrencyList`).
- **Page — faithful port of the old customer screen (list + add/edit modal on one page):**
  - **List:** search toolbar (name/contact person, client-side), paginated table (default 100, sizes 100/150/200 — matches the old app's `[100,150,200]`), skeleton loading, empty state. Columns: Customer Name, Contact Person (FirstName + LastName), City, Country, **Status badge** (success Active / neutral Inactive), **Prospect badge** (info Prospect / success Customer), single Edit action (pencil icon button, same as old).
  - **Add/Edit modal** (ng-bootstrap, `size:'lg'`): **Is Individual** + **Prospect** switch row; Customer Name*; **Billing Address** block (Address*, City, Zip, Country* → loads states on change, State, Currency*); **Contact Person** block (Name*, Last Name, Designation, Phone, Email, Web); **Status** block (Status Inactive/Active, **VAT registered** switch, **Customer Code*** shown only when `!IsIndividual`, **Tax No. (TRN)*** shown only when `VATEligible`); **Shipping Address** block with **"Same as billing address"** toggle (default on; when off, shows address/city/zip/country/state; toggling on copies Billing into Shipping with `AddressId=0`).
  - **Save:** posts the form with `SiteUserId = userService.profileId` and `CustomerTypeValue = 2` (mirrors the old app); on `MessageTypeValue===1` closes the modal + refreshes the list, else toasts the backend text. Validation toasts mirror the old checks (customer name, customer code unless individual, billing address, billing country, currency, contact name, VAT no if eligible, shipping address if not same-as-billing).
  - **Reused** `EqTable`/`EqPaginator`/`EqBadge`/`EqToolbar`/`ToastService` + the global `_forms.scss` form classes and `_customers.scss` switch/badge styles (already scaffolded).

### Create / Edit Quote built (`src/app/features/quotes/pages/create-quote/`)

- **Route:** `app.routes.ts` — `/createquote` (new) and `/createquote/:id` (edit) now load the real `CreateQuote` component (both were placeholders). Edit/Copy land with `TaxInvoiceId`; copy is distinguished by `?copy=1` (from the list's Copy action).
- **Model:** `models/invoice.model.ts` — extended `QuoteModel` with the save/detail fields the page needs (`QuoteDate`/`QuoteDateString`, `QuoteRevNumber`, `QuoteType`, `DiscountRate`/`DiscountAmount`, `VATType`, `PreTaxTotal`/`TotalVAT`/`InvoiceTotal`, `CreatedBy`); `QuoteOption.QuoteId` and `QuoteDetailsModel.QuoteId` became `number | null`; added `QuoteLineItem` (internal line shape incl. shared `VatCodeId` + computed `PreTaxAmount`/`TaxAmount`) + `QuoteRevisionModel` + `emptyQuote()` / `emptyQuoteLine()` factories.
- **Service:** `invoice.service.ts` — added `addUpdateQuote()` (`Invoice/AddUpdateQuote`, posts the `QuoteModel`) + `getQuoteRevisions()` (`Invoice/getQuoteRevisions`).
- **Page — faithful port of the old create-quote screen:**
  - **Document type:** `Quote`/`Proforma` radios (hard-coded 1/2, matching the old app) — disabled when editing a saved quote, re-enabled in copy mode.
  - **Customer:** select auto-fills attention/address/TRN/currency/rate/place-of-supply + min date from customer invoice-start date (same as invoice).
  - **Document Details:** — Quote/Proforma No. (read-only `QuoteNumber + QuoteRevNumber`), LPO Number, Date, Currency→read-only Rate, Payment Terms (default 4), Quote/Proforma Type (invoice-type parameter list), Sales Executive, Division, Place of Supply, and a **single header VAT code** select — the quote shares ONE VAT code across all lines (old behavior).
  - **Lines:** add/remove, product pick → desc/UOM autofill, no per-line discount column (old quote dropped it); per-line VAT column binds to the same shared `vatCodeId`.
  - **Tax math (matches old quote, NOT the invoice branch):** per-line `PreTax = Qty×Rate`, `Tax = PreTax×VATRate`, `Amount = PreTax+Tax`; then the **header-level discount** (`DiscountRate` % or `DiscountAmount` flat via the rate parameter select) is applied to the sum: `aggPreTax = sum−discount`, `aggTax = aggPreTax×VATRate`, `aggTotal = aggPreTax+aggTax`. No tax-inclusive/simplified math (quotes are always additive), and **the saved totals reflect the discounted pretax** exactly like the old `CalculateTaxAmount()`. Base→AED conversion via the form's `ConversionRate`.
  - **Line-summary table** (right-aligned, old quote style): Discount (%, AMT) input+select row, Pre Tax Total, VAT, Total — in the selected `CurrencySymbol` plus a "Base (AED)" column when the currency isn't AED.
  - **Summary card:** Pre Tax / Tax / Total in AED, extra base-currency block when foreign, Discount row when > 0, **Save** action + **Print Quote** (only when editing, not copy).
  - **Terms & Conditions:** TinyMCE editor (same as invoice).
  - **Revisions:** when editing, loads `getQuoteRevisions(id)` and shows Version (`QuoteNumber`) / Date / Document (Print PDF via `GenerateQuote` → `/Documents/Quote/{file}`) — a table the old app had too (Version had a fixed field-name bug there, `QuoteRevNumber`; ours uses the revision's real `QuoteNumber`).
  - **Copy:** header **Copy** button (edit mode only) → confirm modal → `copyQuote()`: `isCopy=true`, `QuoteId`/`QuoteNumber`/`QuoteRevNumber` nulled, line `QuoteDetailId`s nulled, toast. Same-route copy like the invoice page. The list's `?copy=1` does the same on load.
  - **Save** posts the whole form → `AddUpdateQuote`; toast + `router.navigate(['quotelist'])`. Validation toasts mirror the old invoice-form checks (customer, date, currency, payment term, type, executive, division, place-of-supply, VAT code, lines, terms).
  - Quotes are **always editable** — the old create-quote had no per-quote `IsEditable` gating (unlike invoices), so no read-only/`editable=` param on this route.
   - **Show total switch (2026-09-01):** added the `Show total` toggle below the Items summary table, bound to a new `QuoteModel.showtotal` field (default `true`, loaded from the saved quote, saved with it). It's a print-time preference: the backend `GenerateQuote` PDF hides the totals / amount-in-words / "Total Payable" block when `showtotal` is false (`InvoiceService.cs:964,1251,1278,1323,1335`). Toggling + printing a saved quote results in a PDF without totals — matching the old create-quote screen (which had the same switch under the items table, `[(ngModel)]="InvoiceData.showtotal"`, default true).
   - **VAT select label fixed (2026-09-01):** the option text was `{{ VATCodeName }} {{ VATRate }}%` and the backend's `VATCodeName` already carries the rate (e.g. `5%`, `0%`, `Exempt 0%`) → the dropdown read `5%  5%`. Now shows just `{{ VATCodeName }}`, exactly like the old app's `{{VATCode.VATCodeName}}` in both the Document Details VAT select and the per-line VAT select. **The create-invoice page had the same duplicated pattern and was fixed the same way (2026-09-02)** — its per-line VAT select now shows `{{ v.VATCodeName }}` only.

### Quote List built (`src/app/features/quotes/pages/quote-list/`)

- **Route:** `app.routes.ts` — `/quotelist` loads the real `QuoteList` component (was a placeholder). The `/createquote` and `/createquote/:id` placeholder routes it navigates to are now the real Create/Edit Quote page (see above).
- **Model:** `QuoteListModel` added to `models/invoice.model.ts` — mirrors the backend Entity exactly, including the string-typed `InvoiceRevNumber`, `Quotetype`/`QuotetypeText`/`QuotetypeValue`, nullable `InvoiceTotal`/`TotalVAT`/`InvoiceDate`. (The old template rendered `{{row.QuoteRevNumber}}`, which doesn't exist — that's an old-app bug; the real field is `InvoiceRevNumber`, not rendered.)
- **Service:** `invoice.service.ts` — added `getQuoteList()` (`Invoice/GetQuoteList`) + `generateQuote()` (`Invoice/GenerateQuote`). Year list reuses the existing `getInvoiceYears()` (`Invoice/getInvoiceYear`).
- **Page:** search + Year toolbar (**Division / Customer / Type filters added 2026-09-01** — the old quote list has none; options are derived client-side from the loaded list via `divisionOptions`/`customerOptions`/`typeOptions`, filtered in `filteredList`, persisted in the same `QuoteFilters` localStorage blob), paginated table (default 100, 50/100/150), skeleton loading, empty state.
- **Pagination column-width fix (2026-09-01):** the Date column wrapped `10-01-2025` across 3 lines on page 2. `.eq-table` uses `table-layout:auto`, so widths are recomputed per page's visible rows. Fixed by adding `.eq-table td.eq-nowrap { white-space: nowrap }` (quote-list.scss) on the date cell + a `width: '90px'` hint on the Date column.
- **Columns:** Quote/Proforma Number (mono, with the type appended as `<small> - Proforma</small>` — the separate Type column was removed for a tighter view), Date (`eq-nowrap`), Customer (link → `/createquote/:id`), Value, Tax (both `CurrencySymbol`-prefixed, 2dp), Division (plain text — old list has no division icon here), Product, Sales Executive, action dropdown.
- **Row actions:** Edit Quote → `/createquote/:id`, Copy Quote → `/createquote/:id?copy=1` (the create-quote page will interpret it, mirroring the old `invoiceIdcopy` localStorage flag), Print Quote → `Invoice/GenerateQuote` then opens `/Documents/Quote/{file}` using the runtime `ConfigService.fileBaseUrl`.

### Create / Edit Invoice built (`src/app/features/invoices/pages/create-invoice/`)

- **Model:** `models/invoice.model.ts` — added `InvoiceFormModel`, `InvoiceLineItem` (+ all master-data types: `CurrencyModel`, `PaymentTermModel`, `ParameterModel`, `AccountLedgerModel`, `UOMModel`, `VATCodeModel`, `StateModel`, `ProductModel`, `CustomerDetail`, `QuoteOption`, `QuoteDetailsModel`, `QuoteModel`, wrapper `R*List` types), `emptyInvoiceForm()`, `emptyLineItem()`
- **Service:** `services/invoice.service.ts` — added the create/edit endpoints (currencies, payment terms, parameters, account ledgers, UOMs, VAT codes, products, customers-full, `getInvoicesList`, customer quotes, quotes list, `addUpdateInvoice`)
- **Page:** `pages/create-invoice/` — full port of the old create/edit screen:
  - **New:** customer select auto-fills address/attention/TRN/currency/rate/place-of-supply and the quote dropdown (load quote → pre-fill form + lines)
  - **Edit (`/createinvoice/:id`):** loads via `GetInvoicesList`, back-solved discount mode, status badge (Cancelled/Paid/Due), Print + Cancel Invoice buttons
  - **Lines:** add/remove rows, product pick → description/UOM/VAT-rate autofill, per-line discount (percent/flat via discount-mode), live tax recalc; VAT-code-grouped summary table under the grid
  - **Tax math:** faithful port of the old formulas — std branch sums per VAT code then totals; simplified branch (`IsTaxInclusive || VATType===1`) back-solves tax incl. `SimplifiedVATCodeId`/`SimplifiedDiscountRateId`
   - **Dates:** ng-bootstrap datepickers (Invoice/Delivery/Payment) with `d/M/yyyy` strings + ISO built at save; min invoice date from customer invoice start date (**payment date is NOT auto-computed** — see the payment-date fix below)
  - **Terms & Conditions:** TinyMCE editor (`@tinymce/tinymce-angular` v9 `EditorComponent`)
  - Save posts the whole form to the misspelled `AddUpdateInvoive` endpoint; toast + redirect to list
- **Routes:** `app.routes.ts` — `createinvoice` and `createinvoice/:id` (lazy)
- **TinyMCE wiring:** `angular.json` copies `node_modules/tinymce` → `assets/tinymce`; `app.config.ts` provides `TINYMCE_SCRIPT_SRC` = `assets/tinymce/tinymce.min.js`
- **Styles:** reused global `_forms.scss` classes (`eq-invoice-*`, `eq-form-card`, `eq-line-table`, `eq-summary-*`, `eq-editor-slot`); component scss adds only base `select` styling + `.eq-required`

### Create/Edit Invoice — recent fixes & additions

- **Loader freeze fixed:** the form stayed stuck on "Loading invoice form…" even though all 13 boot endpoints returned 200. Root cause was change detection not re-rendering after `loading=false`; forced with `ChangeDetectorRef.detectChanges()` in `ngOnInit`'s forkJoin next/error and in `loadInvoice`. Also added per-group `timeout(15000)` + `catchError→of(null)` + 30s safety cap (all still useful for resilience).
- **Edit-load VAT-code mapping fixed (`normalizeRow`):** the backend DTO sends `VATCodeId` (capital) but the model read `VatCodeId` — every loaded line's VAT code became 0, `calculateTax()` skipped all lines, totals stayed 0, and the status badge fell into the "Cancelled" heuristic (`InvoiceTotal==0 && TotalVAT==0`). `normalizeRow` now reads `VATCodeId ?? VatCodeId` (plus `InvoiceVatRate` fallback) → summary shows real amounts and Paid/Due badge is correct.
- **Header meta** now mirrors the old app: `Paid/Due/Cancelled badge · Customer Name · Invoiced {date} · Read-only badge` in edit mode (via `invoiceDateLabel` getter). Back button text → "Back to Invoices".
- **Line table** header uses the old `num`/`actions` column classes; removed the fixed `<colgroup>` pixel widths and set `min-width:0` + `table-layout:auto` locally so the table no longer forces page-level horizontal scroll.
- **Icons fixed (`_icons.scss`):** added the missing `icon-*::before { content }` glyph mappings for Simple Line Icons (the old app's bundle had them, our port only copied the `@font-face` + family rule, so `<i class="icon-doc">` rendered empty). Added `.eq-icon-btn.dropdown-toggle::after { display:none }` to drop the ng-bootstrap caret on the kebab button.

### Copy Invoice (from edit page header)

- **Trigger:** header **Copy** button on the edit page (`isEdit && !isCancelled && !isCopy`), opens an ng-bootstrap confirm modal ("Do you want to copy this invoice?"), **Yes** → `copyInvoice()`.
- **Mechanism (matches old app — no route change, stays on `/createinvoice/:id`):** sets `isCopy=true`, nulls `TaxInvoiceId`, `InvoiceNumber`, and the Invoice/Delivery/Payment dates (`*Date` + `*DateString`), and zeroes each line's `TAXInvoiceDetailId`/`TaxInvoiceId`. Customer, lines, and FooterText are kept. Because `TaxInvoiceId` is null and line ids are 0, `submit()` posts to the same `AddUpdateInvoive` endpoint and the backend creates a brand-new invoice.
- **UI in copy mode:** header title "Copy of {num}", Save button reads "Save Invoice", Print and Cancel Invoice buttons hidden (`isEdit && !isCopy`). Dates cleared for the user to re-enter (old app behavior).
- **Blocked for cancelled invoices** (button not shown + `copyInvoice()` guards on `isCancelled`).
- **Cancelled-status fix:** `loadInvoice` captures `cancelledOnLoad` from the backend header (`InvoiceTotal==0 && TotalVAT==0`) *before* `calculateTax()` runs, and `isCancelled` uses that for loaded invoices. Previously `calculateTax()` recomputed totals from the line `Qty/Rate` on load, resurrecting non-zero totals on a cancelled invoice → it showed "Paid" and the Copy button. New (`loaded=false`) and copy invoices are never treated as cancelled.
- **Copy clears all dates (incl. payment):** `form.Payment*` and `paymentDateStruct` are nulled, so the saved copy posts a null payment date (matches old app, which nulls it in both `CopyItem` and `getInvoice`). Added a `setTimeout(cdr.detectChanges())` so the ng-bootstrap datepicker inputs visibly clear after the confirm modal closes.
- **Payment date is NOT auto-computed (matches old app):** removed `applyPaymentDate()` auto-fill from `onInvoiceDatePicked`/`onPaymentTermPicked`. The old app's `SetPaymentDate()` is defined but never invoked, and its payment-date input has no change handler — so changing the invoice date or payment term does NOT fill the payment date there. Ours now only sets the payment date when the user picks it (or parses the stored value on edit-load). Payment-date min/max bounds relaxed to the global invoice-date range (2017-06-10 → today) to match the old app.
- **KPI date-parsing fix (invoice list):** the KPI cards were not counting newly created/copied invoices. Root cause: the KPIs parsed `InvoiceDateString` (the `d/M/yyyy` display string) with `new Date()`, which fails on that format → the invoice was excluded from "Invoiced this month" / "Outstanding (YR)". Fixed to parse the ISO `InvoiceDate` field (`parseDate(row.InvoiceDate) ?? parseDate(row.InvoiceDateString)`), matching the old app which parses `item.InvoiceDate` for KPI math and uses the string only for display. Same applied to `PaymentRecdDate` for "Paid this month". Removed the now-unused `extractYear()`.
- **Outstanding KPI converts to AED via saved per-invoice `ConversionRate`:** the "Outstanding (YR …)" card now sums `InvoiceTotal * (ConversionRate || 1)` over non-cancelled, unpaid, same-year invoices, so USD (or any other-currency) invoices are converted to AED at the rate recorded on the invoice. The backend list (`GetInvoiceList`) already returns `ConversionRate` per row (old app `InvoiceService.cs:792`); added `ConversionRate` to the frontend `InvoiceListModel` to consume it. "Invoiced this month" / "Paid this month" remain raw (not currency-converted) until requested otherwise.

**Known deliberate deviations from the old app:** native `<select>` (no ng2-select), no "amount in words" display, `calculateTax()` silently skips incomplete rows (tight checks live in `validate()`, which toasts), no role-based edit-locking / usertype gating (no user/login system yet — copy is shipped ungated exactly like the old app's copy/print, which were not role-gated; Save/Cancel usertype gating deferred to a future step).

### Shared components built (`src/app/shared/components/`)

| Component | File | Selector | Purpose |
|---|---|---|---|
| `EqBadge` | `eq-badge/eq-badge.ts` | `eq-badge` | Status badge (`tone`, `compact` inputs) |
| `EqToolbar` | `eq-toolbar/eq-toolbar.ts` | `eq-toolbar` | Filter bar wrapper |
| `EqDropdown` | `eq-dropdown/eq-dropdown.ts` | `eq-dropdown` | Kebab row-action menu (ng-bootstrap) |
| `EqPaginator` | `eq-paginator/eq-paginator.ts` | `eq-paginator` | Table footer with pagination (ng-bootstrap) |
| `EqTable` | `eq-table/eq-table.ts` | `eq-table` | Table shell with skeleton loading |

### Shared services built (`src/app/shared/services/`)

| Service | File | Purpose |
|---|---|---|
| `ToastService` | `toast.service.ts` | Lightweight toast notifications (success/error/warning) |

### Invoice List built (`src/app/features/invoices/`)

- **Model:** `models/invoice.model.ts` — `InvoiceListModel`, `BusinessDivision`, `SalesExecutive`, `CustomerOption`, `InvoiceMessage`, `PaymentModel`, `deriveStatus()`, `KpiCard`
- **Service:** `services/invoice.service.ts` — all 7 API endpoints (list, years, divisions, executives, customers, generate PDF, add payment)
- **Page:** `pages/invoice-list/` — full list with:
  - Page head with "New Invoice" button
  - Toolbar (search + year/status/division/customer filters + clear button)
  - KPI cards (Outstanding / Invoiced this month / Paid this month)
  - Table (9 columns + action dropdown + inline status badges + division icons)
  - Row actions: Edit (always), Record Payment (due only, opens datepicker modal), Print (non-cancelled, opens PDF in new tab)
  - Pagination (100 default, 50/100/150 page sizes)
  - Empty state
  - Skeleton loading
  - Filter persistence via localStorage (save on change, restore on load, delete after restore)
  - Record Payment modal (ng-bootstrap datepicker, invoice context, min=invoicedate max=today, d/M/yyyy format)
- **Route:** `app.routes.ts` — `invoicelist` loads the real component

### User area (login, role-driven menu, role gating, change password)

- **Session store:** `core/models/user.model.ts` (`UserDetail`, `LoginModel`, `LoginResponse`, `ChangePasswordModel`) + `core/services/user.service.ts` (`UserService`) — reads/writes `localStorage.userDetails`, exposes `userDetail`, `roleId`, `profileId`, `isLoggedIn`, `login()`, `changePassword()`, `logout()`.
- **Login page:** `features/login/` — port of the old direct-login path: POST `Common/GetLogin`, on success persist the `Login` payload and route to `invoicelist`; on `Error` toast the message. (**Central-login SSO `GetLoginCDB` NOT ported** — direct `GetLogin` only, per decision.)
- **Routes/guard:** `app.routes.ts` — `/login` under the (now router-outlet-hosting) `AuthLayout`; the `MainLayout` subtree is wrapped in a new `authGuard` (`core/guards/auth.guard.ts`) that redirects unauthenticated users to `/login`. Added `changepassword` route. Also added placeholder `customer` and `employee` routes so the DB menu `PageUrl`s (`/customer`, `/employee`) navigate instead of falling through to the dashboard wildcard. (Menu `PageUrl`→route: `/dashboard`, `/invoicelist`, `/quotelist` , `/customer`, `/employee` all resolve; `/master,{id},{name}` routes via the comma-splitting `onMenuClick`.)
- **Role-driven menu (data-driven grouping + icons):** `main-layout.ts` `loadMenu()` calls `Common/GetSiteuserMenuList` with `userService.roleId` (falls back to `1` only if not logged in). Grouping is **no longer hardcoded**: `buildMenuGroups()` splits `salesMenus`/`masterMenus` by `ParentMenu.MenuGroup === 'Sales'` (else Master). Sidebar icons bind to `ParentMenu.CssClass` (real `icon-*` from the API) — the `menuIcons` name→icon map and name-based Sales/Master split were removed from `main-layout.ts`/`sidebar.ts`. **Confirmed role table (DB `Role`):** `1=Admin, 2=Manager, 3=Executive, 4=SuperAdmin` (Role 4 = SuperAdmin, NOT admin). `RoleId`→`MenuRole`: Admin(1)/SuperAdmin(4)=full set, Manager(2)=Sales only, **Executive(3)=empty sidebar**.
- **Profile header:** `main-layout/profile/` now renders the real user from `UserService` (name, role, formatted `LastLogin`) and gained a **Change Password** menu item; logout clears `userDetails` (not `localStorage.clear()`) and goes to `/login`.
- **In-page edit gating (Create/Edit Invoice):** `create-invoice.ts` `canEdit` = `invoiceEditable === 1` for saved edits (new/copy drafts always editable). `invoiceEditable` mirrors the old app's per-invoice `IsEditable` hand-off: the invoice **list** passes the row's `IsEditable` (from `GetInvoiceList`, type `number` 0/1) to the edit route via the `editable` query param, read in `ngOnInit`. **Default is now `0` (locked)** so an edit reached without an explicit `editable=1` renders read-only — matching the old app, where a missing `IsEditable` counts as `!= 1` → locked. The invoice **list**'s customer-name link (which previously navigated to `/createinvoice/:id` with NO `editable` param) now passes the same `[queryParams]` as the dropdown Edit action. Template: the `eq-readonly` class (`pointer-events:none` over the layout, re-enabling `.eq-summary-actions`) locks the form; **Save** + **Cancel Invoice** are hidden when `!canEdit`; **Copy** (header, outside the layout) and **Print** (summary, re-enabled) are NOT permission-gated — shown for any non-cancelled existing invoice, matching the old app where `IsCancel==false` is the only gate. So editing a locked invoice is a read-only view with Copy + Print but no Save/Cancel, for any user. The invoice **list** row actions are unchanged (old list doesn't gate them by role). (The old app's `roleId==4` "always editable" branch was dropped after checking: in the live system the admin's actual `RoleId` is not 4, so the only thing that ever unlocks a locked invoice is `IsEditable==1` itself.)
- **Sidebar section titles:** `sidebar.html` now wraps both `SALES` and `MASTER DATA` group titles in `@if (…)` so an empty group renders no header. This fixes, e.g., managers/roles with only Sales menus no longer showing a dangling "MASTER DATA" title (and executives/roles with no menu at all show an empty sidebar — that's data-driven by `RoleId` → `MenuRole`, not a frontend bug).
- **Change Password page:** `features/login/change-password/` — POST `Common/ChangePassword` with `{ userId: profileId, OldPassword, NewPassword, ConfirmPassword }` (matches the backend `ChangePasswordModel` and its SP `ChangePassword(userId, OldPassword, NewPassword)`; `OldPassword` is encrypted server-side). Validates old + new entered and new==confirm, toasts `MessageTypeValue` result.
- **Login + Change Password UI refresh (2026-09-01):** emQube logo image (`/media/emqube-logo-black.svg` — served from `public/media/`) replaces the "EmQube" text brand; compact right-aligned `eq-btn-sm` Sign In button; show/hide-password eye toggle (`fa-eye`/`fa-eye-slash`, `.eq-password-wrap` + `.eq-password-toggle` button) added to the login password field and all three change-password fields (each with its own `passVisible` state); login + change-password inputs now use the shared `.eq-field` input style (added `input[type="password"]`/`[type="email"]` to the `_forms.scss` `.eq-field` rule so they match the edit-invoice fields — 38px tall, 13.5px text, `--eq-border`, radius, primary focus ring). Login page background changed to white (`var(--eq-surface)`).
- **Executive/empty-role sidebar hidden (2026-09-01):** when a role has no `MenuRole` rows (e.g. Executive, RoleId 3), the hamburger button, sidebar `<aside>`, and mobile backdrop are all removed from the DOM (`@if (hasMenus)` in `main-layout.html`); a `body.eq-no-sidebar` class is added, and `_shell.scss` overrides `.eq-main`/`.eq-footer` to `margin-left: 0` so the content takes full width. The `hasMenus` getter checks `salesMenus.length > 0 || masterMenus.length > 0`. `body.eq-no-sidebar .eq-header .navbar-brand { width: 160px }` anchors the logo.
- **Profile menu login time:** `login-time pull-left` span inside `.eq-profile-menu-header` (direct child, own row via `flex-basis:100%` on the now `flex-wrap:wrap` header, with a top border separator). `loginTime` getter in `profile.ts` formats `LastLogin` as `d/M/yyyy, h:mm:ss am/pm`.
- **UAT/LIVE runtime config (2026-09-01):** added `public/config.json` holding `{ apiUrl, fileBaseUrl }`, loaded at bootstrap by the new `ConfigService` (`core/services/config.service.ts`) via an `APP_INITIALIZER` wired in `app.config.ts`. `ApiService.post` now uses `configService.apiUrl`; both print consumers (`invoice-list.ts`, `create-invoice.ts`) use `configService.fileBaseUrl` for `/Documents/Invoice/{file}`. Falls back to `environment.apiUrl` (and `/api`-stripped origin) if `config.json` is missing. **Same build now targets UAT or LIVE by editing/deploying `config.json`** (the file is emitted un-processed to the build root from `public/`).

## Standing rules for this project (until told otherwise)

- **Do not modify anything in this project without an explicit go-ahead for that specific change.** Reviewing/reading is always fine; writing, deleting, or scaffolding is not, until told.
- **Do not modify the old app** (`d:\projects\.net\01\emqubeinvoice2025\EmqubeInvoice\`) as part of this rewrite. It's the reference/spec. If a bug there needs fixing, that's its own separate, explicitly-requested change.
- No Angular Material — thin components wrapping `.eq-*` CSS (see `UI-DESIGN-SYSTEM.md`, `DECISIONS.md`).
- Build order starts with Invoice List, not Customer List.
